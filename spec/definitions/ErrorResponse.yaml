type: object
title: ErrorResponse
required:
  - type
  - message
  - request
properties:
  type:
    type: string
    description: 错误类型
    example: BadRequest
  message:
    type: string
    description: 错误详情
    example: something error
  request:
    type: string
    description: 当前请求 url
    example: /api/demo
