type: object
required:
  - user_id
  - password
  - transfer_used
  - transfer_enable
  - is_admin
  - is_locked
properties:
  user_id:
    type: string
    example: 1301010101
  password:
    type: string
    example: password
  transfer_used:
    type: string
    example: 0 B
  transfer_enable:
    type: string
    example: 0 B
  is_admin:
    type: boolean
    example: false
  is_locked:
    type: boolean
    example: false
