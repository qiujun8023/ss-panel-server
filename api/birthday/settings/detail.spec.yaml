get:
  x-app: birthday
  tags: [ birthday ]
  summary: 获取提醒设置详情详情
  security:
  - wechat: []
  parameters:
    - name: setting_id
      in: query
      type: integer
      required: true
      description: 设置 Id
  responses:
    200:
      description: 设置列表
      schema:
        $ref: "#/definitions/BirthdaySetting"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
