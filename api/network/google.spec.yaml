get:
  tags: [ network ]
  summary: Google
  description: 判断 Google 是否可以访问
  responses:
    200:
      description: 返回访问耗时
      x-examples:
        {
          result: true,
          time: "800ms"
        }
      schema:
        properties:
          result:
            type: boolean
            example: true
          time:
            type: string
            example: "800ms"
    default:
      description: 错误
      x-examples:
        {
          type: "BadGateway",
          message: "Get google timeout after 3000ms",
          request: "/api/network/google"
        }
      schema:
        $ref: "#/definitions/ErrorResponse"
