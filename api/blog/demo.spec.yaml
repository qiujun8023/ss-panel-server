get:
  tags: [ blog ]
  summary: 博客加速
  description: 使用 UPYUN 进行博客加速
  parameters:
    - name: user_id
      in: query
      required: false
      type: string
      description: 示例、无意义
  responses:
    200:
      description: 返回成功信息
      schema:
        $ref: "#/definitions/SuccessResponse"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
post:
  tags: [ blog ]
  summary: 博客加速
  description: 使用 UPYUN 进行博客加速
  parameters:
    - name: user_id
      in: query
      required: false
      type: string
      description: 示例、无意义
  responses:
    200:
      description: 返回成功信息
      schema:
        $ref: "#/definitions/SuccessResponse"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
