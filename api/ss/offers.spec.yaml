get:
  x-app: ss
  tags: [ ss ]
  summary: 获取赞助列表
  security:
  - wechat: []
  responses:
    200:
      description: 赞助列表
      schema:
        type: array
        items:
          $ref: "#/definitions/ssOffer"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
