get:
  x-app: ss
  tags: [ ss ]
  summary: 获取服务列表
  security:
  - wechat: []
  responses:
    200:
      description: 服务列表
      schema:
        type: array
        items:
          $ref: "#/definitions/ssService"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
