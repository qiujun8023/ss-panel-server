get:
  x-app: ss
  tags: [ ss ]
  summary: 获取服务详情
  security:
  - wechat: []
  parameters:
    - name: service_id
      in: query
      type: integer
      required: true
      description:  服务 ID
  responses:
    200:
      description: 服务详情
      schema:
        $ref: "#/definitions/ssService"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
