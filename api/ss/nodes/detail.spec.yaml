get:
  x-app: ss
  tags: [ ss ]
  summary: 获取节点详情
  security:
  - wechat: []
  parameters:
    - name: node_id
      in: query
      type: integer
      required: true
      description: 节点 ID
  responses:
    200:
      description: 节点详情
      schema:
        $ref: "#/definitions/ssNode"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
