get:
  x-app: ss
  tags: [ ss ]
  summary: 获取用户详情
  security:
  - wechat: []
  parameters:
    - name: user_id
      in: query
      type: integer
      required: true
      description: 用户 ID
  responses:
    200:
      description: 用户详情
      schema:
        $ref: "#/definitions/ssUser"
    default:
      description: 错误
      schema:
        $ref: "#/definitions/ErrorResponse"
